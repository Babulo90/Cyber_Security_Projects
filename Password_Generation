import random
import string
def generate_password(lenght: int=10):
    alphabet = string.ascii_letters + string.ddigits + string.punctuations
    password = ''.join(random.choice(alphabet) for i in range(length))
    return password
password = generate_password
print(f"generate_password: {password}")
